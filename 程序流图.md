# 系统结构设计文档编写流程图

```mermaid
flowchart TD
Start([开始]) --> A[确定文档结构]
A --> B[文档概述编写]
B --> B1[编写文档背景]
B --> B2[确定文档目标]
B --> B3[定义适用范围]
B --> B4[说明文档结构]
B1 & B2 & B3 & B4 --> C[设计目标和原则]
C --> C1[系统设计目标]
C --> C2[系统设计原则]
C --> C3[用户体验需求]
C1 & C2 & C3 --> D[设计约束和限制]
D --> D1[技术限制分析]
D --> D2[资源限制评估]
D --> D3[法律合规限制]
D --> D4[预算时间限制]
D1 & D2 & D3 & D4 --> E[逻辑视点设计]
E --> E1[功能模块划分]
E --> E2[模块关系设计]
E --> E3[数据流设计]
E --> E4[接口API设计]
E --> E5[数据库设计]
E1 & E2 & E3 & E4 & E5 --> F[开发视点设计]
F --> F1[代码结构设计]
F --> F2[组件交互设计]
F --> F3[技术栈选择]
F1 & F2 & F3 --> G[运行视点设计]
G --> G1[运行环境设计]
G --> G2[性能优化设计]
G --> G3[监控方案设计]
G --> G4[故障处理设计]
G1 & G2 & G3 & G4 --> H[UML图设计]
H --> H1[用例图]
H --> H2[类图]
H --> H3[时图]
H --> H4[状态图]
H --> H5[部署图]
H1 & H2 & H3 & H4 & H5 --> I[文档审核]
I --> J{是否通过}
J -->|否| K[修改完善]
K --> I
J -->|是| L[文档完成]
L --> End([结束])
```

# 系统部署图

## 1. 基础部署架构
```mermaid
graph TD
    subgraph 用户层
        A1[PC浏览器]
        A2[移动设备]
    end

    subgraph 负载均衡层
        B[Nginx负载均衡]
    end

    subgraph Web服务器集群
        C1[Web Server 1]
        C2[Web Server 2]
    end

    subgraph 应用服务器集群
        D1[App Server 1]
        D2[App Server 2]
    end

    subgraph 数据层
        E1[(主数据库)]
        E2[(从数据库)]
        E3[(Redis缓存)]
    end

    A1 & A2 --> B
    B --> C1 & C2
    C1 & C2 --> D1 & D2
    D1 & D2 --> E1
    E1 --> E2
    D1 & D2 --> E3
```

## 2. 网络拓扑部署图
```mermaid
graph TD
    subgraph 互联网区域
        A[互联网用户]
    end

    subgraph DMZ区
        B[防火墙]
        C[负载均衡器]
        D[Web服务器]
    end

    subgraph 应用区
        E[应用服务器]
        F[缓存服务器]
    end

    subgraph 数据区
        G[数据库服务器]
        H[备份服务器]
    end

    A --> B
    B --> C
    C --> D
    D --> E
    E --> F
    E --> G
    G --> H
```

## 3. 高可用部署架构
```mermaid
graph TD
    subgraph 接入层
        A1[防火墙主]
        A2[防火墙备]
    end

    subgraph 负载均衡层
        B1[LB主]
        B2[LB备]
    end

    subgraph 应用层
        C1[应用服务器1]
        C2[应用服务器2]
        C3[应用服务器3]
    end

    subgraph 数据层
        D1[(主库)]
        D2[(从库1)]
        D3[(从库2)]
    end

    subgraph 缓存层
        E1[Redis主]
        E2[Redis从]
    end

    A1 & A2 --> B1 & B2
    B1 & B2 --> C1 & C2 & C3
    C1 & C2 & C3 --> D1
    D1 --> D2 & D3
    C1 & C2 & C3 --> E1
    E1 --> E2
```

## 4. 监控系统部署图
```mermaid
graph TD
    subgraph 监控对象
        A1[应用服务器]
        A2[数据库服务器]
        A3[缓存服务器]
    end

    subgraph 监控系统
        B1[Prometheus]
        B2[Grafana]
        B3[AlertManager]
    end

    subgraph 告警通道
        C1[邮件]
        C2[短信]
        C3[钉钉]
    end

    A1 & A2 & A3 --> B1
    B1 --> B2
    B1 --> B3
    B3 --> C1 & C2 & C3
```

# 系统整体体系架构图

## 1. 系统分层架构
```mermaid
graph TD
    subgraph 表现层
        A1[Web前端]
        A2[移动端]
        A3[管理后台]
    end

    subgraph 应用层
        B1[用户模块]
        B2[业务模块]
        B3[系统模块]
    end

    subgraph 服务层
        C1[用户服务]
        C2[业务服务]
        C3[系统服务]
        C4[公共服务]
    end

    subgraph 数据层
        D1[MySQL]
        D2[Redis]
        D3[文件存储]
    end

    subgraph 基础设施层
        E1[服务器]
        E2[网络]
        E3[安全]
        E4[监控]
    end

    A1 & A2 & A3 --> B1 & B2 & B3
    B1 & B2 & B3 --> C1 & C2 & C3 & C4
    C1 & C2 & C3 & C4 --> D1 & D2 & D3
    D1 & D2 & D3 --> E1 & E2 & E3 & E4
```

## 2. 系统功能架构
```mermaid
graph TD
    subgraph 公共访问功能
        A1[招生计划查询]
        A2[录取情况查询]
        A3[考生录取查询]
        A4[智能咨询服务]
    end

    subgraph 工作人员功能
        B1[考生信息管理]
        B2[通知书管理]
        B3[分班管理]
    end

    subgraph 管理员功能
        C1[用户管理]
        C2[权限管理]
        C3[系统配置]
        C4[日志管理]
    end

    subgraph 基础功能
        D1[认证授权]
        D2[数据备份]
        D3[系统监控]
        D4[安全防护]
    end

    A1 & A2 & A3 & A4 --> D1 & D2 & D3 & D4
    B1 & B2 & B3 --> D1 & D2 & D3 & D4
    C1 & C2 & C3 & C4 --> D1 & D2 & D3 & D4
```

## 3. 技术架构
```mermaid
graph TD
    subgraph 前端技术栈
        A1[Vue.js]
        A2[Element UI]
        A3[Axios]
        A4[Vuex]
    end

    subgraph 后端技术栈
        B1[Spring Boot]
        B2[MyBatis]
        B3[Spring Security]
        B4[Redis]
    end

    subgraph 数据存储
        C1[MySQL]
        C2[Redis]
        C3[文件系统]
    end

    subgraph 运维支撑
        D1[Nginx]
        D2[Docker]
        D3[Jenkins]
        D4[Prometheus]
    end

    A1 & A2 & A3 & A4 --> B1 & B2 & B3 & B4
    B1 & B2 & B3 & B4 --> C1 & C2 & C3
    C1 & C2 & C3 --> D1 & D2 & D3 & D4
```

## 4. 安全架构
```mermaid
graph TD
    subgraph 安全防护层
        A1[WAF]
        A2[防火墙]
        A3[IPS/IDS]
    end

    subgraph 访问控制层
        B1[认证中心]
        B2[权限控制]
        B3[会话管理]
    end

    subgraph 数据安全层
        C1[数据加密]
        C2[数据脱敏]
        C3[数据备份]
    end

    subgraph 安全审计层
        D1[操作日志]
        D2[安全审计]
        D3[风险监控]
    end

    A1 & A2 & A3 --> B1 & B2 & B3
    B1 & B2 & B3 --> C1 & C2 & C3
    C1 & C2 & C3 --> D1 & D2 & D3
```

## 5. 系统分层详细架构
```mermaid
graph TD
    subgraph 外部接口层
        A1[Web接口]
        A2[移动接口]
        A3[第三方接口]
        A4[开放API]
    end

    subgraph 表示层
        B1[Web界面]
        B2[移动界面]
        B3[管理界面]
    end

    subgraph 应用逻辑层
        C1[业务控制器]
        C2[业务服务]
        C3[业务规则]
        C4[业务流程]
    end

    subgraph 数据访问层
        D1[DAO接口]
        D2[ORM映射]
        D3[数据缓存]
        D4[事务管理]
    end

    subgraph 数据持久层
        E1[关系数据库]
        E2[缓存数据库]
        E3[文件存储]
        E4[数据备份]
    end

    subgraph 安全层
        F1[认证授权]
        F2[访问控制]
        F3[数据加密]
        F4[安全审计]
    end

    subgraph 日志层
        G1[操作日志]
        G2[系统日志]
        G3[安全日志]
        G4[审计日志]
    end

    subgraph 基础设施层
        H1[配置管理]
        H2[异常处理]
        H3[工具类]
        H4[公共组件]
    end

    A1 & A2 & A3 & A4 --> B1 & B2 & B3
    B1 & B2 & B3 --> F1 & F2 & F3 & F4
    F1 & F2 & F3 & F4 --> C1 & C2 & C3 & C4
    C1 & C2 & C3 & C4 --> D1 & D2 & D3 & D4
    D1 & D2 & D3 & D4 --> E1 & E2 & E3 & E4
    
    C1 & C2 & C3 & C4 --> G1 & G2 & G3 & G4
    F1 & F2 & F3 & F4 --> G1 & G2 & G3 & G4
    
    H1 & H2 & H3 & H4 --> A1 & A2 & A3 & A4
    H1 & H2 & H3 & H4 --> B1 & B2 & B3
    H1 & H2 & H3 & H4 --> C1 & C2 & C3 & C4
    H1 & H2 & H3 & H4 --> D1 & D2 & D3 & D4
```

## 6. 数据访问层详细架构
```mermaid
graph TD
    subgraph DAO层
        A1[StudentDAO]
        A2[ClassDAO]
        A3[UserDAO]
        A4[SystemDAO]
    end

    subgraph ORM映射层
        B1[实体映射]
        B2[关系映射]
        B3[查询映射]
    end

    subgraph 缓存层
        C1[本地缓存]
        C2[分布式缓存]
        C3[缓存同步]
    end

    subgraph 数据源层
        D1[主数据源]
        D2[从数据源]
        D3[读写分离]
    end

    subgraph 事务管理
        E1[事务定义]
        E2[事务传播]
        E3[事务隔离]
    end

    A1 & A2 & A3 & A4 --> B1 & B2 & B3
    B1 & B2 & B3 --> C1 & C2 & C3
    C1 & C2 & C3 --> D1 & D2 & D3
    D1 & D2 & D3 --> E1 & E2 & E3
```

## 7. 清晰的系统分层架构
```mermaid
graph TB
    subgraph 外部接口层 Interface Layer
        I1[Web接口]
        I2[移动接口]
        I3[第三方接口]
    end

    subgraph 表示层 Presentation Layer
        P1[Web界面]
        P2[移动界面]
        P3[管理界面]
    end

    subgraph 业务层 Business Layer
        B1[业务控制器 Controller]
        B2[业务服务 Service]
        B3[业务规则 Rules]
    end

    subgraph 数据访问层 Data Access Layer
        D1[DAO接口]
        D2[ORM映射]
        D3[数据缓存]
    end

    subgraph 数据持久层 Persistence Layer
        DB1[关系数据库]
        DB2[缓存数据库]
        DB3[文件存储]
    end

    subgraph 横切关注点 Cross-Cutting Concerns
        direction LR
        C1[安全层 Security]
        C2[日志层 Logging]
        C3[配置层 Configuration]
        C4[异常处理 Exception]
    end

    I1 & I2 & I3 --> P1 & P2 & P3
    P1 & P2 & P3 --> B1 & B2 & B3
    B1 & B2 & B3 --> D1 & D2 & D3
    D1 & D2 & D3 --> DB1 & DB2 & DB3

    C1 -.-> I1 & I2 & I3
    C1 -.-> P1 & P2 & P3
    C1 -.-> B1 & B2 & B3
    C1 -.-> D1 & D2 & D3
    
    C2 -.-> I1 & I2 & I3
    C2 -.-> P1 & P2 & P3
    C2 -.-> B1 & B2 & B3
    C2 -.-> D1 & D2 & D3

    C3 -.-> I1 & I2 & I3
    C3 -.-> P1 & P2 & P3
    C3 -.-> B1 & B2 & B3
    C3 -.-> D1 & D2 & D3

    C4 -.-> I1 & I2 & I3
    C4 -.-> P1 & P2 & P3
    C4 -.-> B1 & B2 & B3
    C4 -.-> D1 & D2 & D3

    classDef layer fill:#f9f,stroke:#333,stroke-width:4px
    class I1,I2,I3,P1,P2,P3,B1,B2,B3,D1,D2,D3,DB1,DB2,DB3 layer
```

主要特点：
1. 清晰的层次结构：
   - 外部接口层：处理所有外部请求
   - 表示层：负责用户界面展示
   - 业务层：处理核心业务逻辑
   - 数据访问层：提供数据操作接口
   - 数据持久层：实现数据持久化

2. 横切关注点：
   - 安全层：权限控制、认证授权
   - 日志层：系统日志、操作日志
   - 配置层：系统配置管理
   - 异常处理：统一异常处理

3. 层次间通信：
   - 实线表示直接依赖
   - 虚线表示横切关注点的影响范围

4. 每层职责明确：
   - 单一职责原则
   - 高内聚低耦合
   - 接口隔离
   - 依赖倒置

## 8. 优化后的系统分层架构
```mermaid
graph TB
    subgraph 外部接口层 Interface Layer
        direction TB
        I1[Web接口]
        I2[移动接口]
        I3[第三方接口]
        I1 --- I2 --- I3
    end

    subgraph 表示层 Presentation Layer
        direction TB
        P1[Web界面]
        P2[移动界面]
        P3[管理界面]
        P1 --- P2 --- P3
    end

    subgraph 业务层 Business Layer
        direction TB
        B1[业务控制器<br>Controller]
        B2[业务服务<br>Service]
        B3[业务规则<br>Rules]
        B1 --- B2 --- B3
    end

    subgraph 数据访问层 Data Access Layer
        direction TB
        D1[DAO接口]
        D2[ORM映射]
        D3[数据缓存]
        D1 --- D2 --- D3
    end

    subgraph 数据持久层 Persistence Layer
        direction TB
        DB1[关系数据库]
        DB2[缓存数据库]
        DB3[文件存储]
        DB1 --- DB2 --- DB3
    end

    subgraph 横切关注点 Cross-Cutting Concerns
        direction TB
        C1[安全层<br>Security]
        C2[日志层<br>Logging]
        C3[配置层<br>Configuration]
        C4[异常处理<br>Exception]
        C1 --- C2 --- C3 --- C4
    end

    I1 & I2 & I3 --> P1 & P2 & P3
    P1 & P2 & P3 --> B1 & B2 & B3
    B1 & B2 & B3 --> D1 & D2 & D3
    D1 & D2 & D3 --> DB1 & DB2 & DB3

    C1 -.-> I1
    C1 -.-> P1
    C1 -.-> B1
    C1 -.-> D1

    C2 -.-> I2
    C2 -.-> P2
    C2 -.-> B2
    C2 -.-> D2

    C3 -.-> I3
    C3 -.-> P3
    C3 -.-> B3
    C3 -.-> D3

    classDef layer fill:#f9f,stroke:#333,stroke-width:2px
    class I1,I2,I3,P1,P2,P3,B1,B2,B3,D1,D2,D3,DB1,DB2,DB3 layer
```

主要改进：
1. 每个子图内部采用竖向排列
2. 减少横向连接线
3. 增加文字间距
4. 使用<br>实现文字换行
5. 简化横切关注点的连接线
6. 调整节点大小和间距

这样的布局更加清晰，文字更容易阅读，层次关系更加明显。如果您觉得还需要进一步调整，我可以继续优化。
